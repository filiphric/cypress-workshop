{
  "name": "cypress-example-vue-vuex-rest",
  "version": "1.0.0",
  "description": "Testing Vue + Vuex + REST TodoMVC using Cypress",
  "scripts": {
    "open": "cross-env open http://localhost:3000",
    "start": "./node_modules/.bin/json-server --static . data.json -m middleware.js --port 3000",
    "reset": "node reset-db.js",
    "reset:db": "npm run reset",
    "reset:database": "npm run reset"
  },
  "dependencies": {
    "json-server": "0.15.1"
  },
  "devDependencies": {
    "cross-env": "6.0.3"
  }
}
